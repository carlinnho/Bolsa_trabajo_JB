info:
  name: Login
  type: http
  seq: 1

http:
  method: POST
  url: "{{baseUrl}}/api/auth/?action=login"
  params:
    - name: action
      value: login
      type: query
  body:
    type: json
    data: |-
      {
        "correo": "carlos.prueba@gmail.com",
        "password": "Password123*"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
