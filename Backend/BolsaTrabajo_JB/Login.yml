info:
  name: Login
  type: http
  seq: 1

http:
  method: POST
  url: "{{baseUrl}}/api/auth/?action=login"
  params:
    - name: action
      value: login
      type: query
  body:
    type: json
    data: |-
      {
        "correo": "admin@gmail.com",
        "password": "Carlinnho$$20"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
